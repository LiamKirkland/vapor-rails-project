<ul class="game-grid">
  <% games.each do |game| %>
    <li>
      <%= link_to game_path(game), class: "game-card" do %>
        <%= image_tag game.display_cover_image, width: 140, height: 196, class: "cover-thumb" %>
        <span class="game-caption"><%= game.name %> (<%= game.release_year %>)</span>
      <% end %>
    </li>
  <% end %>
</ul>